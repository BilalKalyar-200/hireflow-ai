"""
HireFlow AI — Pipeline Routes (Step 3).

POST /pipeline/run — trigger autonomous pipeline for a job
GET /pipeline/status/{job_id} — get current pipeline progress
WebSocket /ws/pipeline — real-time stage updates to dashboard
"""
