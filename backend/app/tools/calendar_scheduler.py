"""
HireFlow AI — Calendar Scheduler Tool (Step 3).

Input: candidate email, interview datetime, duration
Output: Google Calendar event ID and Meet link
Uses Google Service Account JSON — no OAuth.
"""
